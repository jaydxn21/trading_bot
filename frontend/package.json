{
  "name": "quantumtrader-frontend",
  "version": "7.1.0",
  "private": true,
  "dependencies": {
    "lightweight-charts": "^4.2.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1",
    "socket.io-client": "^4.8.1"
  },
  "scripts": {
  "start": "react-scripts start",
  "build": "react-scripts build",
  "dev": "npm start",
  "deploy": "npm run build && rm -rf ../static && mkdir -p ../static && rsync -av build/static/ ../static/ && node scripts/inject.js && echo 'DASHBOARD LIVE'"
},
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all",
      "not IE 11"
    ],
    "development": [
      "last 2 chrome version",
      "last 2 firefox version",
      "last 2 safari version",
      "last 2 edge version"
    ]
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2"
  }
}